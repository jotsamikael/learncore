package com.dodibo.learncore.role;

/*
 * This enum is used to define the level of the role.
 * It is used to define the level of the role.
 * PLATFORM: The role is a platform role.
 * TENANT: The role is a tenant role.
 */
public enum RoleLevel {
    PLATFORM,
    TENANT
}
