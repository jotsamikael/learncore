/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageCategoryResponse } from '../../models/page-category-response';

export interface GetCategories$Params {
  name?: string;
  description?: string;
  page?: number;
  size?: number;
  sortBy?: string;
  sortDirection?: 'ASC' | 'DESC';
}

export function getCategories(http: HttpClient, rootUrl: string, params?: GetCategories$Params, context?: HttpContext): Observable<StrictHttpResponse<PageCategoryResponse>> {
  const rb = new RequestBuilder(rootUrl, getCategories.PATH, 'get');
  if (params) {
    rb.query('name', params.name, {});
    rb.query('description', params.description, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('sortDirection', params.sortDirection, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageCategoryResponse>;
    })
  );
}

getCategories.PATH = '/categories';
